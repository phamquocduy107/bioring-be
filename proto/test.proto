syntax = "proto3";

package test;

service TestService {
  rpc Ping (PingRequest) returns (PingResponse);
}

message PingRequest {
  string data = 1;
}

message PingResponse {
  bool pong = 1;
  string receivedAt = 2;
  string data = 3;
}
