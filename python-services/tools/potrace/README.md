# Potrace (Windows)

Đặt `potrace.exe` vào thư mục này để personalization_engine xuất SVG:

```text
python-services/tools/potrace/potrace.exe
```

Tải: http://potrace.sourceforge.net/

Hoặc cài potrace và thêm vào PATH, hoặc set `POTRACE_PATH` trong `.env`.
