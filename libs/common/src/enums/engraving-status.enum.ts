export enum EngravingStatus {
  PENDING = 'PENDING',
  CAPTURED = 'CAPTURED',
  APPROVED = 'APPROVED',
  REJECTED = 'REJECTED',
}

export enum EngravingBioMetricStatus {
  PENDING_CAPTURE = 'PENDING_CAPTURE',
  CAPTURED = 'CAPTURED',
  PROCESSING = 'PROCESSING',
}
